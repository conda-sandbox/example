name: News fragment

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Check News
        uses: conda/actions/check-news@c76832d8727b116dc6d6395a0676c1836067ef95
        with:
          skip-label: no-news
          require-pr-number: true
          news-directory: news
          exempt-authors: >-
            pre-commit-ci[bot],dependabot[bot],conda-bot,github-actions[bot],conda-sandbox-bot
