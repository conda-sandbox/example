name: News fragment

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Check News
        uses: conda/actions/check-news@eb3dfe6018a3d9bf86637b2140f2c16d4c8a3b4a
        with:
          skip-label: no-news
          require-pr-number: true
          fragment-format: sectioned
          news-directory: news
          exempt-authors: >-
            pre-commit-ci[bot],dependabot[bot],conda-bot,github-actions[bot],conda-sandbox-bot
