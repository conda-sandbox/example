name: Tests

on:
  push:
    branches:
      - main
      - '[0-9].*.x'
      - '[0-9][0-9].*.x'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke
        run: echo "ok"
